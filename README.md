# Local AI Hub — Веб-дашборд и агентная студия локального ИИ

Минималистичный высокопроизводительный интерфейс для локальных нейросетей, мониторинга аппаратных ресурсов оборудования, управления моделями актуальной базы 2026 года с поддержкой всех уровней квантования и встроенным автономным агентом разработки уровня Claude Code / Open Codex.

---

## Ключевые возможности

### 1. Аппаратный мониторинг в реальном времени (1s LIVE)
- **Процессор (CPU)**: поточное вычисление загрузки всех ядер системы, пиковые и средние значения, сглаженные графики Безье на Canvas Retina.
- **Память (RAM)**: точное определение занятого объема, проводной памяти и степени давления (Memory Pressure) через системные вызовы ядра macOS (`vm_stat`).
- **GPU & Apple Metal**: расчет нагрузки аппаратного ускорителя инференса и используемой видеопамяти VRAM.
- **Температура оборудования (°C)**: непрерывный мониторинг термального состояния чипа с цветовой адаптацией (Оптимальная до 65°C, Повышенная 65-80°C, Критическая от 80°C) и графиком температурной динамики.
- **Хранилище диска**: отображение свободного пространства под веса локальных GGUF-моделей.
- **Master Timeline Chart**: расширенный 60-секундный синхронный график загрузки процессора и оперативной памяти с возможностью мгновенной очистки буфера.

### 2. Каталог актуальных моделей осени 2026 года
- **Рассуждения (Reasoning & Chain-of-Thought)**:
  - DeepSeek-R2 7B / 14B — передовая логика и пошаговый синтез гипотез.
  - QwQ 3.5 32B — олимпиадный уровень решения алгоритмических задач от Alibaba.
  - Upstage Solar Pro 22B — компактный reasoning-гигант с архитектурой Depth-Up-Scaling.
  - Phi-5 7B & Phi-4 14B — высокоточные компактные модели от Microsoft.
  - DeepSeek-R1 7B — классический эталон открытых reasoning-сетей.
- **NVIDIA Nemotron & Исследования ИИ**:
  - NVIDIA Nemotron-Mini 4B — ультра-оптимизированная модель для диалогов и edge-инференса.
  - NVIDIA Nemotron-4 15B — синтетические данные высшей фильтрации для глубокого анализа.
  - NVIDIA Nemotron 70B — лидер открытых лидербордов с превосходным следованием системным инструкциям.
  - NVIDIA NVLM 16B Vision — мультимодальная модель NVIDIA для чтения графиков, таблиц и OCR.
- **THUDM GLM-4 & CodeGeeX (Zhipu AI)**:
  - THUDM GLM-4 9B — флагман с контекстом 128k, глубоким знанием русского языка и Tool Calling.
  - THUDM GLM-Edge 4B — компактная энергоэффективная версия для мгновенного отклика.
  - CodeGeeX-4 9B (CodeGLM) — специализированная открытая модель для кодинга и рефакторинга.
  - THUDM GLM-4 Voice 9B — мультимодальный анализ визуальных сцен и аудио-транскриптов.
- **Программирование и Агенты**:
  - Qwen 3.8 Coder 9B / 27B — флагман 2026 года для автономных агентов и Tool Calling.
  - DeepSeek Coder V2 16B MoE — Mixture-of-Experts архитектура (активирует 2.4B параметров).
  - Qwen 3.5 Coder 7B / 14B — поддержка 100+ языков, монорепозиториев и генерации diff.
  - Codestral 2 14B — архитектурный кодер от Mistral AI.
- **Линейка Google Gemma 3 & 4 (2026)**:
  - Google Gemma 4 2B / 9B / 27B — архитектура Gemini 2.0 с глубокой поддержкой русского языка.
  - Google Gemma 3 1B / 4B / 12B — оптимальный баланс скорости и качества.
- **Чат, RAG и Корпоративные модели**:
  - Meta Llama 4 3B / 8B / 70B — открытый стандарт индустрии 2026 года.
  - Qwen 3.8 4B / 9B / 27B — универсальная мультиязычная эрудиция.
  - Cohere Command R 35B — флагман для корпоративного RAG и работы со сложной документацией.
  - IBM Granite 3.1 8B — корпоративная надежность, безопасность и чистота обучающих данных.
- **Тренды Hugging Face и Независимые Лаборатории**:
  - Hugging Face SmolLM2 1.7B / 360M — флагманские компактные модели HF, обученные на 11 трлн токенов.
  - Nous Hermes 3 8B — популярнейший независимый тюнинг на базе Llama 3.1 с нейтральным выравниванием и надежным Tool Calling.
  - Mistral Small 3 24B & Ministral 8B — открытые frontier-модели от Mistral AI с чередующимся вниманием (sliding window).
  - Ai2 OLMo 2 7B — 100% прозрачная открытая наука от Allen Institute for AI с открытым датасетом Dolma.
  - Cohere Aya Expanse 8B — лидер мультиязычности на 23+ языках от исследовательской лаборатории Cohere For AI.
  - Moonshot AI Moonlight 16B MoE — открытая MoE-архитектура от создателей Kimi.
  - Liquid LFM 3B — не-трансформерная архитектура Liquid Foundation Model с постоянным расходом памяти.
  - LG EXAONE 3.5 7.8B — флагманская открытая модель исследовательского института LG.
  - Sentence Transformers MiniLM L6 — абсолютный рекордсмен Hugging Face (>500 млн скачиваний) для сверхбыстрого векторного поиска.
- **Мультимодальные (Vision)**:
  - Gemma 4 Vision 9B, Llama 4 Vision 12B, NVIDIA NVLM 16B, GLM-4 Voice 9B, Pixtral 2 14B.
- **Векторный поиск и RAG**:
  - Nomic Embed v2, BAAI BGE-M3, Sentence Transformers MiniLM L6.

### 3. Прямая интеграция с реестром Hugging Face
В строке ручной загрузки поддерживается прямая подгрузка любых GGUF-моделей сообщества (bartowski, TheBloke, Qwen, Unsloth) через префикс `hf.co/`:
```bash
hf.co/bartowski/Qwen2.5-Coder-7B-Instruct-GGUF
hf.co/NousResearch/Hermes-3-Llama-3.1-8B-GGUF
```
Дашборд автоматически скачивает слои, регистрирует веса в локальном хранилище и подключает модель в Playground и Agent Studio.

### 4. Полная поддержка всех уровней квантования (GGUF)
В карточке каждой модели доступен селектор квантования, динамически пересчитывающий размер загружаемого файла и требуемый объем RAM:
- `Q2_K`: 2-bit (ультра-сжатие, ~50% от размера Q4, для минимального объема памяти).
- `Q3_K_M`: 3-bit (высокая скорость инференса, ~72% от размера Q4).
- `Q4_K_M`: 4-bit (рекомендуемый баланс точности и скорости, стандарт Ollama).
- `Q5_K_M`: 5-bit (повышенная точность при минимальной потере перплексии, ~122% от Q4).
- `Q6_K`: 6-bit (премиум-качество, близкое к оригинальным весам, ~145% от Q4).
- `Q8_0`: 8-bit (максимальная точность квантования, ~190% от Q4).
- `FP16`: 16-bit Float16 (исходные несжатые веса для референсного качества).

### 5. Claude Code / Open Codex — Автономный агент разработки
Встроенная среда выполнения задач программной инженерии на локальных моделях:
- **Автономный цикл ReAct**: планирование -> вызов инструмента -> наблюдение результата -> правка кода -> финальный ответ.
- **Локальные инструменты**:
  - `run_command`: выполнение консольных команд в рабочей директории проекта.
  - `read_file`: построчное чтение исходных кодов и конфигураций.
  - `write_file`: создание новых файлов.
  - `patch_file`: атомарное внесение изменений с генерацией построчного Git Diff.
  - `list_dir`: сканирование дерева проекта.
  - `grep_search`: мгновенный поиск ключевых слов и шаблонов.
- **4 роли агента**: Software Architect / Coder, QA Engineer, Security Auditor, DevOps Assistant.
- **Визуализация**: интерактивный терминал с кодами завершения и цветной Git Diff (`+` зеленый, `-` красный).

### 6. Модели без цензуры (Uncensored & Abliterated) и режим полного обхода ограничений
В веб-интерфейсе реализована встроенная поддержка моделей со снятыми этическими ограничениями и прямой обход системных эвристик отказа:
- **Модели с вырезанными векторами отказа (Abliteration)**:
  - `dolphin-3:8b` (Dolphin 3.0 Llama 3.1 8B Uncensored) — лидер открытых некорпоративных моделей от Эрика Хартфорда (Cognitive Computations).
  - `dolphin-mistral:7b` (Dolphin 2.9.4 Mistral 7B) — ультра-быстрая и дерзкая модель с полным снятием ограничений.
  - `llama-3-uncensored:8b` (Llama 3.1 8B Abliterated) — хирургически модифицированные веса по методике Лабонна со 100% точностью и нулевыми отказами.
  - `gemma-2-uncensored:9b` (Gemma 2 9B Abliterated) — глубокие технические и научные знания без нравоучений и морализаторства.
  - `qwen2.5-coder-uncensored:7b` (Qwen 2.5 Coder 7B Abliterated) — модель для пентестинга, анализа уязвимостей и эксплоитов без ложных срабатываний безопасности.
  - `wizardlm-uncensored:13b` (WizardLM 13B Uncensored) — классическая логическая модель на базе Evol-Instruct без цензуры.
- **Режим принудительного обхода (Bypass Mode)**:
  - Быстрый тумблер **«Без цензуры»** в панели чата и агента разработки.
  - При активации сервер внедряет высокоприоритетную директиву снятия ограничений и адаптирует параметры семплинга, гарантируя прямые, объективные и полные ответы на любые технические вопросы.

### 7. Полнофункциональное меню настроек в веб-интерфейсе
По кнопке **«Настройки»** в шапке доступна панель полного управления инференсом и локальным рантеймом:
- **Параметры семплинга**:
  - **Температура (Temperature)**: плавный ползунок от 0.0 до 2.0 с шагом 0.05 и живым индикатором (от детерминированного кода до высокой креативности).
  - **Top-P (Nucleus Sampling)**: регулировка вероятностного усечения токенов (0.05 – 1.0).
  - **Top-K**: лимит кандидатов генерации (1 – 100).
  - **Штраф за повторы (Repeat Penalty)**: предотвращение зацикливаний и эха (1.0 – 2.0).
  - **Контекстное окно (`num_ctx`)**: выбор глубины памяти от 2 048 до 131 072 токенов (2K, 4K, 8K, 16K, 32K, 64K, 128K).
- **Директивы и промпты**:
  - Кастомная директива обхода цензуры (Anti-Refusal Jailbreak/Bypass Directive) с возможностью редактирования.
  - Стандартный системный промпт помощника.
  - Переключатель «Включить режим обхода по умолчанию».
- **Сетевое подключение и агент**:
  - Сетевой хост Ollama API (`http://127.0.0.1:11434` или удаленный сервер).
  - Лимит шагов Claude Code агента по умолчанию (4, 6, 8, 10, 15).
- **Персистентность**: все параметры мгновенно сохраняются в локальном хранилище браузера (`localStorage`) и применяются ко всем последующим сессиям.

### 8. Дизайн и Темы оформления
- **Светлая и Темная темы**: переключение в один клик с сохранением выбора в `localStorage`.
- **Строгая эстетика**: все элементы интерфейса выполнены на базе чистых векторных SVG-иконок без использования смайликов.
- **Zero-Dependencies**: сервер написан на чистом стандартном API Node.js. Не требует установки тяжелых внешних npm-пакетов.

---

## Требования к системе

- **Операционная система**: macOS (Apple Silicon M1/M2/M3/M4 или Intel), Linux (Ubuntu, Debian, Fedora, Arch) или Windows (WSL2 / PowerShell).
- **Среда выполнения**: Node.js версии 18.0 или выше.
- **Движок инференса (рекомендуется)**: Ollama версии 0.5.x или выше.
  *(Если Ollama не установлена или временно выключена, интерфейс автоматически переходит в интерактивный демонстрационный режим для тестирования графиков, чата и анимаций агента).*

---

## Установка и быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/nenubics/web_ui_for_local_ai.git
cd web_ui_for_local_ai
```

### 2. Запуск дашборда
Для запуска не требуется выполнять `npm install`, так как проект использует стандартные модули платформы Node.js (`http`, `fs`, `os`, `child_process`, `path`):

```bash
node server.js
```
или с помощью скрипта запуска:
```bash
chmod +x start.sh
./start.sh
```

После запуска откройте в браузере адрес:
```
http://localhost:3000
```

### 3. Установка и запуск Ollama (для реальных локальных моделей)

#### macOS (Homebrew):
```bash
brew install ollama
ollama serve
```

#### Linux:
```bash
curl -fsSL https://ollama.com/install.sh | sh
ollama serve
```

#### Windows:
Скачайте официальный инсталлятор с сайта [ollama.com](https://ollama.com) и запустите службу.

Дашборд автоматически обнаружит работающий сервис Ollama на порту 11434 и обновит статус подключения в шапке.

---

## Конфигурация через переменные окружения

Вы можете переопределить стандартный порт и адрес Ollama:

| Переменная | По умолчанию | Описание |
|---|---|---|
| `PORT` | `3000` | HTTP-порт веб-интерфейса дашборда |
| `OLLAMA_HOST` | `http://127.0.0.1:11434` | Сетевой адрес API локального демона Ollama |
| `WORKSPACE_DIR` | Текущая папка (`cwd`) | Корневая директория для работы автономного агента |

Пример запуска на нестандартном порту:
```bash
PORT=8080 OLLAMA_HOST=http://192.168.1.50:11434 node server.js
```

---

## Руководство по использованию

### Установка модели в 1 клик
1. Откройте вкладку **Мониторинг & Модели**.
2. В карточке нужной модели (например, *Google Gemma 4 9B* или *Qwen 3.8 Coder 9B*) выберите желаемую версию квантования в выпадающем списке (`Q4_K_M`, `Q8_0`, `FP16` и др.).
3. Обратите внимание на расчетный размер весов и требуемый объем RAM.
4. Нажмите кнопку **Установить**.
5. Появится карточка с прогресс-баром, процентом загрузки и скоростью передачи слоев модели в реальном времени.

### Ручная загрузка произвольной модели
В строке консольного ввода в блоке витрины введите название любой модели из реестра Ollama или HuggingFace:
```
gemma-4:27b-q8_0
```
и нажмите кнопку **Скачать**.

### Использование автономного агента (Claude Code / Codex)
1. Перейдите на вкладку **Claude Code (Агент разработки)**.
2. Выберите роль (например, *Claude Code / Software Architect* для разработки или *QA Specialist* для тестов).
3. Выберите модель кодинга (*Qwen 3.8 Coder 9B* или установленную версию) и лимит шагов.
4. Введите задачу, например:
   - *«Проверь структуру директорий проекта и создай скрипт проверки целостности»*
   - *«Найди все функции рендеринга графиков в public/app.js и добавь комментарии»*
5. Нажмите **Запустить агента**.
6. В потоке выполнения в реальном времени отображаются рассуждения, вызовы консольных команд в терминале и превью изменений файлов в формате Git Diff.

### Playground Чат
1. Перейдите на вкладку **Playground Чат**.
2. Выберите установленную модель.
3. Отправьте сообщение. В верхней части окна появится индикатор скорости генерации токенов (`t/s`).

---

## Структура проекта

```
web_ui_for_local_ai/
├── server.js              # Легковесный HTTP/SSE бэкенд на стандартной библиотеке Node.js
├── agent.js               # Движок автономного агента Claude Code (ReAct цикл и инструменты)
├── start.sh               # Быстрый скрипт автозапуска и проверки зависимостей
├── package.json           # Описание проекта и команды запуска
├── README.md              # Документация и руководство пользователя
└── public/                # Клиентская часть веб-приложения
    ├── index.html         # Разметка дашборда (HTML5, SVG-иконки, модальные окна)
    ├── style.css          # Дизайн-система (темная и светлая темы, адаптивная сетка)
    └── app.js             # Клиентский контроллер, графики Retina Canvas, SSE-клиент
```

---

## Безопасность и Приватность

- Все вычисления, инференс и обработка контекста происходят **исключительно на вашей локальной машине**.
- Никакие промпты, исходные коды проекта и аппаратные метрики не передаются во внешние облачные сервисы или сторонние API.

---

## Лицензия

Распространяется под свободной лицензией MIT. Подходит для личного и коммерческого использования.
